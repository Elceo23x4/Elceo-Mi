'use client';
import { useSearchParams } from 'next/navigation';
import { useState } from 'react';
export function ResetPasswordClient(){const token=useSearchParams().get('token')??'';const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState<string|null>(null);return <main className="elceo-auth-page"><section className="elceo-auth-shell"><div className="elceo-auth-actions"><p className="elceo-kicker">SECURE RECOVERY</p><h1>Choose a new password</h1><label className="elceo-auth-label"><span>New password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="elceo-auth-label"><span>Confirm password</span><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button className="elceo-pill-button elceo-pill-button-auth" onClick={async()=>{if(password!==confirm){setError('Passwords do not match');return;}const response=await fetch('/api/auth/password-reset/confirm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password}),referrerPolicy:'no-referrer'});if(response.ok){window.location.assign('/login?passwordReset=complete');return;}setError('This reset link is invalid or expired');}}>Reset password</button>{error?<p className="elceo-auth-error">{error}</p>:null}</div></section></main>}
