import {NextResponse} from 'next/server';
import {StripeSandboxPaymentProviderAdapter,recoverCanonicalPaymentIntention,resolvePaymentProviderMode} from '@elceo/application-state';
import {requireAppUserState} from '../../../../lib/auth/session';
import {captureError} from '../../../../lib/monitoring';
import {getRequestId} from '../../../../lib/request-context';
export async function GET(request:Request){const requestId=getRequestId(request);try{const{session}=await requireAppUserState();const intention=await recoverCanonicalPaymentIntention(session.user.id);if(!intention)return NextResponse.json({intention:null,newIntentionAllowed:true},{headers:{'x-request-id':requestId,'cache-control':'no-store'}});let checkoutUrl:string|null=null;if(intention.checkoutContinuationAvailable&&intention.providerSessionReference&&resolvePaymentProviderMode()==='sandbox_provider'){const provider=await new StripeSandboxPaymentProviderAdapter().retrievePaymentOrSession(intention.providerSessionReference);checkoutUrl=provider.checkoutUrl??null;}return NextResponse.json({intention:{operationId:intention.operationId,state:intention.state,checkoutContinuationAvailable:Boolean(checkoutUrl),checkoutUrl,reconciliationRequired:intention.reconciliationRequired,succeeded:intention.succeeded,newIntentionAllowed:intention.newIntentionAllowed}},{headers:{'x-request-id':requestId,'cache-control':'no-store'}})}catch(error){captureError('api.billing.intention',error,{requestId});const persistence=error instanceof Error&&error.message==='payment_persistence_unavailable';return NextResponse.json({error:{code:persistence?'payment_persistence_unavailable':'payment_intention_unavailable',message:persistence?'Payment persistence unavailable':'Payment intention unavailable'}},{status:503,headers:{'x-request-id':requestId,'cache-control':'no-store'}})}}
