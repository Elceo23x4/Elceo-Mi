import { parseJsonBody, unwrapValidation, withApiErrorBoundary, jsonSuccess } from '@/lib/server/api';
import { requireAuthenticatedSubject } from '@/lib/server/auth';
import { getApplicationStateRuntime } from '@/lib/server/composition';
import { auditInternalMutation, completeSecurityDecision, failSecurityDecision, requireSecurityDecision } from '@/lib/server/security';
import { validatePositionUpdateRequest } from '@elceo/schemas';
export const GET = withApiErrorBoundary(async (_request: Request, context: { params: Promise<{ positionId: string }> }) => { await requireAuthenticatedSubject(); const { positionId } = await context.params; const replay = await getApplicationStateRuntime().portfolio.getPortfolioEntityReplay('position', positionId); if (!replay) throw new Error('not_found'); return jsonSuccess({ position: replay.current, replay }); });
export const PATCH = withApiErrorBoundary(async (request: Request, context: { params: Promise<{ positionId: string }> }) => { const subject = await requireAuthenticatedSubject(); const { positionId } = await context.params; const patch = unwrapValidation(validatePositionUpdateRequest(await parseJsonBody(request))); const actor = { actorKind: 'user' as const, actorId: subject.userId, subjectId: subject.subjectId }; const security = await requireSecurityDecision({ request, routePath: '/api/portfolio/positions/[positionId]', method: 'PATCH', actionKind: 'portfolio_position_write', actor, subjectId: subject.subjectId, requestBody: patch }); if (!security.ok) return security.response; try { const position = await getApplicationStateRuntime().portfolio.updatePosition(positionId, patch, { actorKind: 'user', actorId: subject.userId }); const envelope = { ok: true as const, data: { position } };
 await completeSecurityDecision({ decision: security.decision, idempotencyKey: security.idempotencyKey, responseBody: { position }, responseEnvelope: envelope, httpStatus: 200, requestHash: security.requestHash }); await auditInternalMutation({ actor, subjectId: subject.subjectId, actionKind: 'portfolio_position_write', routePath: '/api/portfolio/positions/[positionId]', method: 'PATCH', request, idempotencyKey: security.idempotencyKey }); return jsonSuccess({ position }); } catch (error) { await failSecurityDecision({ idempotencyKey: security.idempotencyKey, errorMessage: error instanceof Error ? error.message : 'unknown_error' }); throw error; } });
