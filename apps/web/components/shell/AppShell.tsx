import { TopNav } from './TopNav';
import { IdentityFrame } from '../branding/IdentityFrame';

export function AppShell() {
  return (
    <main>
      <TopNav />
      <IdentityFrame />
    </main>
  );
}
