import type { SecurityAuditEvent, SecurityBlockReason, SecurityDecision, SecurityDecisionStatus, SecurityIdempotencyRecord, SecurityIdempotencyReplayReason, SecurityIdempotencyReplayResult, SecurityIdempotencyResponseRecord, SecurityRateLimitCounter, SecurityRateLimitPolicy, SecurityRuntimeSummary } from '@elceo/types';
import { isEnumValue, isIsoDateString, isNonEmptyString, isObjectRecord, type SchemaValidationResult } from './validation-utils';

const ACTOR_KINDS = ['user', 'internal', 'admin', 'system'] as const;
const ACTION_KINDS = ['account_read','account_write','admin_read','admin_write','internal_mutation','billing_reconcile','billing_policy_evaluate','billing_orchestration_retry','notification_dispatch','refresh_run','workspace_refresh','analytics_generate','coaching_generate','portfolio_snapshot_generate','journal_case_write','journal_case_lifecycle','journal_influence_generate','portfolio_watchlist_write','portfolio_position_write','portfolio_action_write','notification_target_write','notification_subscription_write','notification_verification_issue','notification_verification_consume'] as const;
const WINDOWS = ['minute','hour','day'] as const;
const DECISION_STATUS = ['allowed','blocked','replayed','skipped'] as const;
const BLOCK_REASONS = ['rate_limit_exceeded','idempotency_conflict','invalid_idempotency_key','missing_actor','internal_token_required','admin_access_required','suspicious_replay','unknown'] as const;
const idemStatus = ['started', 'completed', 'failed'] as const;
const nn = (v: unknown): boolean => Number.isInteger(v) && Number(v) >= 0;
const nullableString = (v: unknown): boolean => v === null || typeof v === 'string';
const replayReasons = ['completed_response_found','no_completed_response','request_hash_mismatch','expired','not_found'] as const;
const httpStatus=(v:unknown):boolean=>Number.isInteger(v)&&Number(v)>=100&&Number(v)<=599;

export function validateSecurityRateLimitPolicy(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityRateLimitPolicy> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityRateLimitPolicy must be object`]}; if(!isNonEmptyString(input.policyKey)) e.push(`${pathPrefix}policyKey must be non-empty string`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.window,WINDOWS)) e.push(`${pathPrefix}window is invalid`); if(!nn(input.maxCount)) e.push(`${pathPrefix}maxCount must be integer >= 0`); if(typeof input.subjectScoped!=='boolean') e.push(`${pathPrefix}subjectScoped must be boolean`); if(typeof input.actorScoped!=='boolean') e.push(`${pathPrefix}actorScoped must be boolean`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityRateLimitPolicy}; }
export function validateSecurityIdempotencyRecord(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityIdempotencyRecord> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityIdempotencyRecord must be object`]}; if(!isNonEmptyString(input.idempotencyKey)) e.push(`${pathPrefix}idempotencyKey must be non-empty string`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.actorKind,ACTOR_KINDS)) e.push(`${pathPrefix}actorKind is invalid`); if(!isNonEmptyString(input.actorId)) e.push(`${pathPrefix}actorId must be non-empty string`); if(!isNonEmptyString(input.requestHash)) e.push(`${pathPrefix}requestHash must be non-empty string`); if(!nullableString(input.responseHash)) e.push(`${pathPrefix}responseHash must be string or null`); if(!isEnumValue(input.status,idemStatus)) e.push(`${pathPrefix}status is invalid`); if(!isIsoDateString(input.firstSeenAt)||!isIsoDateString(input.lastSeenAt)||!isIsoDateString(input.expiresAt)) e.push(`${pathPrefix}timestamps must be ISO date strings`); if(typeof input.metadataJson!=='string') e.push(`${pathPrefix}metadataJson must be string`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityIdempotencyRecord}; }
export function validateSecurityRateLimitCounter(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityRateLimitCounter> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityRateLimitCounter must be object`]}; if(!isNonEmptyString(input.counterId)) e.push(`${pathPrefix}counterId must be non-empty string`); if(!isNonEmptyString(input.policyKey)) e.push(`${pathPrefix}policyKey must be non-empty string`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.actorKind,ACTOR_KINDS)) e.push(`${pathPrefix}actorKind is invalid`); if(!isNonEmptyString(input.actorId)) e.push(`${pathPrefix}actorId must be non-empty string`); if(!nullableString(input.subjectId)) e.push(`${pathPrefix}subjectId must be string or null`); if(!isEnumValue(input.window,WINDOWS)) e.push(`${pathPrefix}window is invalid`); if(!isIsoDateString(input.windowStart)||!isIsoDateString(input.windowEnd)||!isIsoDateString(input.updatedAt)) e.push(`${pathPrefix}windowStart/windowEnd/updatedAt must be ISO date strings`); if(!nn(input.count)) e.push(`${pathPrefix}count must be integer >= 0`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityRateLimitCounter}; }
export function validateSecurityDecision(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityDecision> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityDecision must be object`]}; if(!isNonEmptyString(input.decisionId)) e.push(`${pathPrefix}decisionId must be non-empty string`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.actorKind,ACTOR_KINDS)) e.push(`${pathPrefix}actorKind is invalid`); if(!isNonEmptyString(input.actorId)) e.push(`${pathPrefix}actorId must be non-empty string`); if(!nullableString(input.subjectId)) e.push(`${pathPrefix}subjectId must be string or null`); if(!isEnumValue(input.status,DECISION_STATUS)) e.push(`${pathPrefix}status is invalid`); if(!(input.blockReason===null||isEnumValue(input.blockReason as SecurityBlockReason,BLOCK_REASONS))) e.push(`${pathPrefix}blockReason is invalid`); if(!nullableString(input.idempotencyKey)||!nullableString(input.rateLimitPolicyKey)) e.push(`${pathPrefix}idempotencyKey/rateLimitPolicyKey must be string or null`); if(!(input.currentCount===null||nn(input.currentCount))) e.push(`${pathPrefix}currentCount must be integer >= 0 or null`); if(!(input.maxCount===null||nn(input.maxCount))) e.push(`${pathPrefix}maxCount must be integer >= 0 or null`); if(!isIsoDateString(input.decidedAt)) e.push(`${pathPrefix}decidedAt must be ISO date string`); if(typeof input.metadataJson!=='string') e.push(`${pathPrefix}metadataJson must be string`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityDecision}; }
export function validateSecurityAuditEvent(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityAuditEvent> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityAuditEvent must be object`]}; if(!isNonEmptyString(input.auditEventId)) e.push(`${pathPrefix}auditEventId must be non-empty string`); if(!isEnumValue(input.actorKind,ACTOR_KINDS)) e.push(`${pathPrefix}actorKind is invalid`); if(!isNonEmptyString(input.actorId)) e.push(`${pathPrefix}actorId must be non-empty string`); if(!nullableString(input.subjectId)) e.push(`${pathPrefix}subjectId must be string or null`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.decisionStatus,DECISION_STATUS)) e.push(`${pathPrefix}decisionStatus is invalid`); if(!(input.blockReason===null||isEnumValue(input.blockReason as SecurityBlockReason,BLOCK_REASONS))) e.push(`${pathPrefix}blockReason is invalid`); if(!nullableString(input.routePath)||!nullableString(input.method)||!nullableString(input.ipHash)||!nullableString(input.userAgentHash)||!nullableString(input.idempotencyKey)) e.push(`${pathPrefix}nullable string fields invalid`); if(typeof input.metadataJson!=='string') e.push(`${pathPrefix}metadataJson must be string`); if(!isIsoDateString(input.occurredAt)||!isIsoDateString(input.createdAt)) e.push(`${pathPrefix}occurredAt/createdAt must be ISO date strings`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityAuditEvent}; }
export function validateSecurityRuntimeSummary(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityRuntimeSummary> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityRuntimeSummary must be object`]}; if(!isIsoDateString(input.generatedAt)) e.push(`${pathPrefix}generatedAt must be ISO date string`); const keys=['totalAuditEvents','blockedDecisionCount','replayedDecisionCount','rateLimitedCount','idempotencyConflictCount'] as const; for(const k of keys){ if(!nn(input[k])) e.push(`${pathPrefix}${k} must be integer >= 0`);} if(!(input.latestBlockedAt===null||isIsoDateString(input.latestBlockedAt))) e.push(`${pathPrefix}latestBlockedAt must be ISO date string or null`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityRuntimeSummary}; }


export function validateSecurityIdempotencyResponseRecord(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityIdempotencyResponseRecord> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityIdempotencyResponseRecord must be object`]}; if(!isNonEmptyString(input.idempotencyKey)) e.push(`${pathPrefix}idempotencyKey must be non-empty string`); if(!isEnumValue(input.actionKind,ACTION_KINDS)) e.push(`${pathPrefix}actionKind is invalid`); if(!isEnumValue(input.actorKind,ACTOR_KINDS)) e.push(`${pathPrefix}actorKind is invalid`); if(!isNonEmptyString(input.actorId)) e.push(`${pathPrefix}actorId must be non-empty string`); if(!isNonEmptyString(input.requestHash)) e.push(`${pathPrefix}requestHash must be non-empty string`); if(!isNonEmptyString(input.responseHash)) e.push(`${pathPrefix}responseHash must be non-empty string`); if(!httpStatus(input.httpStatus)) e.push(`${pathPrefix}httpStatus must be integer 100-599`); if(typeof input.responseJson!=='string') e.push(`${pathPrefix}responseJson must be string`); if(!isIsoDateString(input.completedAt)||!isIsoDateString(input.expiresAt)) e.push(`${pathPrefix}completedAt/expiresAt must be ISO date strings`); if(typeof input.metadataJson!=='string') e.push(`${pathPrefix}metadataJson must be string`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityIdempotencyResponseRecord}; }
export function validateSecurityIdempotencyReplayResult(input: unknown, pathPrefix = ''): SchemaValidationResult<SecurityIdempotencyReplayResult> { const e:string[]=[]; if(!isObjectRecord(input)) return {ok:false,errors:[`${pathPrefix}SecurityIdempotencyReplayResult must be object`]}; if(typeof input.replayable!=='boolean') e.push(`${pathPrefix}replayable must be boolean`); if(!isNonEmptyString(input.idempotencyKey)) e.push(`${pathPrefix}idempotencyKey must be non-empty string`); if(!(input.httpStatus===null||httpStatus(input.httpStatus))) e.push(`${pathPrefix}httpStatus must be integer 100-599 or null`); if(!nullableString(input.responseJson)) e.push(`${pathPrefix}responseJson must be string or null`); if(!nullableString(input.responseHash)) e.push(`${pathPrefix}responseHash must be string or null`); if(!isEnumValue(input.reason as SecurityIdempotencyReplayReason,replayReasons)) e.push(`${pathPrefix}reason is invalid`); return e.length?{ok:false,errors:e}:{ok:true,value:input as SecurityIdempotencyReplayResult}; }
