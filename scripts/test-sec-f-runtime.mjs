import assert from 'node:assert/strict';
import {logEvent,sanitizeTelemetryValue} from '../packages/config/dist/observability.js';
import {readProviderEnv,validateProviderEnv} from '../packages/schemas/dist/schemas/src/env.schema.js';
const sentinel='SEC_F_SUPER_SECRET';const data={detail:`Authorization: Bearer ${sentinel}`,innocent:`provider failed at https://host/path?token=${sentinel}`,nested:[{value:`password=${sentinel}`}],stack:`https://host/a?api_key=${sentinel}`,contact:`person.${sentinel}@example.com`};assert(!JSON.stringify(sanitizeTelemetryValue(data)).includes(sentinel));let output='';const original=console.error;console.error=x=>output+=String(x);try{logEvent('sec-f','error',`failure token=${sentinel}`,data)}finally{console.error=original}assert(!output.includes(sentinel));
const base={NODE_ENV:'production',APP_STATE_REPOSITORY:'sql',DATABASE_URL:'postgres://example.invalid/db',NOTIFICATIONS_PERSISTENCE_BACKEND:'sql',ANALYTICS_PERSISTENCE_BACKEND:'sql',PAYMENT_PROVIDER_MODE:'disabled',AUTH_SECRET:'fixture',ELCEO_INTERNAL_API_TOKEN:'fixture'};for(const APP_ENV of['staging','production']){assert.equal(validateProviderEnv(readProviderEnv({...base,APP_ENV,NEXT_PUBLIC_APP_BASE_URL:'http://localhost',APP_BASE_URL:'http://localhost',INTERNAL_API_BASE_URL:'http://localhost/api'})).valid,false);assert.equal(validateProviderEnv(readProviderEnv({...base,APP_ENV,NEXT_PUBLIC_APP_BASE_URL:'https://app.example.invalid',APP_BASE_URL:'https://app.example.invalid',INTERNAL_API_BASE_URL:'https://internal.example.invalid'})).valid,true)}assert.equal(validateProviderEnv(readProviderEnv({APP_ENV:'test',NEXT_PUBLIC_APP_BASE_URL:'http://localhost:3000'})).valid,true);console.log('SEC-F telemetry and HTTPS runtime adversarial checks passed');
