import type { BillingRetryCandidate, BillingRetryPlan, BillingReconciliationRun, BillingPolicyTransition } from '@elceo/types';
const hash=(v:string)=>Buffer.from(v).toString('base64').replace(/[^a-zA-Z0-9]/g,'').slice(0,16);
export const buildBillingRetryPlan=(candidate:BillingRetryCandidate, latestRun:BillingReconciliationRun|null, latestPolicy:BillingPolicyTransition|null, nowIso:string):BillingRetryPlan=>{const hasContext=Boolean(candidate.latestReconciliationRunId||candidate.latestPolicyTransitionId); const requiresPolicyReEvaluation=latestPolicy?.recoveredAccess===true || (latestRun?.status==='success'&&(candidate.currentAccountState==='restricted'||candidate.currentPlanKind==='free')); const rationale=hasContext?`retry from ${candidate.reason}${requiresPolicyReEvaluation?' with policy re-evaluation':''}`:'retry rejected due to missing reconciliation/policy context'; return {planId:`borp_${hash(`${candidate.subjectId}:${candidate.reason}:${candidate.latestReconciliationRunId??'none'}:${candidate.latestPolicyTransitionId??'none'}`)}`,subjectKind:'user',subjectId:candidate.subjectId,providerKind:candidate.providerKind,reason:candidate.reason,sourceReconciliationRunId:candidate.latestReconciliationRunId,sourcePolicyTransitionId:candidate.latestPolicyTransitionId,sourceEventId:latestRun?.sourceEventId??null,retryable:hasContext,requiresPolicyReEvaluation,rationale,createdAt:nowIso};};
