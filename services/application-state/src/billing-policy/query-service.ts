import type { BillingPolicySnapshot } from '@elceo/types';
import type { BillingCustomerRepository, BillingLifecycleSubscriptionRepository, BillingPolicyTransitionRepository, AccountEntitlementRepository } from '../persistence';

export class BillingPolicyQueryService { constructor(private customers:BillingCustomerRepository, private subs:BillingLifecycleSubscriptionRepository, private transitions:BillingPolicyTransitionRepository, private entitlements:AccountEntitlementRepository){}
  getLatestBillingPolicyTransition(subjectKind:'user',subjectId:string){return this.transitions.getLatestTransitionForSubject(subjectKind,subjectId);} listRecentBillingPolicyTransitions(subjectKind:'user',subjectId:string,limit?:number){return this.transitions.listRecentTransitionsForSubject(subjectKind,subjectId,limit);} async getBillingPolicySnapshot(subjectKind:'user',subjectId:string):Promise<BillingPolicySnapshot>{ const [customer,subscription,entitlement,latestPolicyTransition]=await Promise.all([this.customers.getCustomerBySubject(subjectKind,subjectId),this.subs.getSubscriptionBySubject(subjectKind,subjectId),this.entitlements.getAccountEntitlement(subjectKind,subjectId),this.transitions.getLatestTransitionForSubject(subjectKind,subjectId)]); if(!entitlement) throw new Error('missing entitlement'); return {generatedAt:new Date().toISOString(),subjectKind,subjectId,customer,subscription,entitlementState:entitlement,latestPolicyTransition}; }}
