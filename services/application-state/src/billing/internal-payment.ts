import { queryDb, withDbTransaction } from '../db/client';

export type InternalPaymentState='created'|'pending_provider'|'processing'|'succeeded'|'failed'|'expired'|'cancelled'|'unknown'|'reconciliation_required'|'refunded'|'partially_refunded'|'reversed'|'chargeback';
export type PaymentProviderMode='disabled'|'local_fake_provider'|'sandbox_stub_for_tests'|'replay_provider_event';
export type FakeProviderOutcome='accepted'|'accepted_response_lost'|'timeout_before_response'|'provider_500_before_accepting'|'provider_500_after_accepting'|'disconnect_during_redirect'|'unknown_result'|'duplicate_provider_reference'|'duplicate_event_id'|'refund'|'partial_refund'|'reversal'|'chargeback';
export type InternalPaymentOperation={internalPaymentOperationId:string;businessIdempotencyKey:string;providerIdempotencyKey:string;subjectUserId:string;targetPlan:string;amount:number;currency:string;provider:string;state:InternalPaymentState;providerPaymentReference:string|null;providerCheckoutSessionReference:string|null;providerEventReferences:string[];version:number;createdAt:string;updatedAt:string;expiresAt:string|null;reconciliationState:'not_required'|'required'|'matched'|'mismatch'|'duplicate_charge_risk';lastProviderComparisonSnapshot:Record<string,unknown>|null;safeErrorCategory:string|null};
type LedgerKind='payment_success'|'refund'|'partial_refund'|'reversal'|'chargeback';
type EntitlementKind='grant'|'refund'|'partial_refund'|'reversal'|'chargeback';
type Inbox={eventId:string;provider:string;kind:string;providerPaymentReference:string|null;operationId:string|null;duplicate:boolean;createdAt:string;payload:Record<string,unknown>};
type PaymentStore={operations:Map<string,InternalPaymentOperation>;byBusiness:Map<string,string>;byProviderKey:Map<string,string>;byPaymentRef:Map<string,string>;bySessionRef:Map<string,string>;inbox:Map<string,Inbox>;ledger:Map<string,unknown>;entitlements:Map<string,unknown>;audit:unknown[];providerCharges:Map<string,string>};
const allowed:Record<InternalPaymentState,InternalPaymentState[]>= {created:['pending_provider','processing','failed','unknown','reconciliation_required','cancelled','expired'],pending_provider:['processing','succeeded','failed','unknown','reconciliation_required','cancelled','expired'],processing:['succeeded','failed','unknown','reconciliation_required','cancelled','expired'],succeeded:['refunded','partially_refunded','reversed','chargeback'],failed:[],expired:[],cancelled:[],unknown:['reconciliation_required','succeeded','failed','expired','cancelled'],reconciliation_required:['succeeded','failed','expired','cancelled','refunded','partially_refunded','reversed','chargeback'],refunded:[],partially_refunded:['refunded','reversed','chargeback'],reversed:[],chargeback:[]};
const terminal=new Set<InternalPaymentState>(['succeeded','failed','expired','cancelled','refunded','partially_refunded','reversed','chargeback']);
function env(){return (globalThis as {process?:{env?:Record<string,string|undefined>}}).process?.env??{};}function id(prefix:string,key:string){let h=0;for(const c of key)h=(h*31+c.charCodeAt(0))>>>0;return `${prefix}_${h.toString(16)}`;}function businessKey(subject:string,key:string){return `business:${subject}:${key}`;}
export function createMemoryPaymentStore():PaymentStore{return {operations:new Map(),byBusiness:new Map(),byProviderKey:new Map(),byPaymentRef:new Map(),bySessionRef:new Map(),inbox:new Map(),ledger:new Map(),entitlements:new Map(),audit:[],providerCharges:new Map()};}
export const sharedMemoryPaymentStore=createMemoryPaymentStore();

export interface InternalPaymentRepository{createOrReuseOperation(input:{subjectUserId:string;targetPlan:string;amount:number;currency:string;businessIdempotencyKey:string;provider:string}):Promise<{operation:InternalPaymentOperation;reused:boolean}>;getOperation(id:string):Promise<InternalPaymentOperation|null>;findByProviderReference(ref:string):Promise<InternalPaymentOperation|null>;findBySessionReference(ref:string):Promise<InternalPaymentOperation|null>;transition(id:string,fromVersion:number,next:InternalPaymentState,patch?:Partial<InternalPaymentOperation>,message?:string):Promise<InternalPaymentOperation>;recordProviderAccepted(id:string,paymentRef:string,sessionRef:string):Promise<InternalPaymentOperation>;insertInbox(event:Inbox):Promise<{inserted:boolean;event:Inbox}>;writeLedgerOnce(op:InternalPaymentOperation,kind:LedgerKind):Promise<boolean>;writeEntitlementOnce(op:InternalPaymentOperation,kind:EntitlementKind):Promise<boolean>;appendAudit(operationId:string|null,message:string,data:Record<string,unknown>):Promise<void>;reconcileLookup(op:InternalPaymentOperation):Promise<{providerPaymentReference:string|null;providerChargeCount:number}>;listUnknown():Promise<InternalPaymentOperation[]>;counts():Promise<{operations:number;providerCharges:number;ledger:number;entitlements:number;inbox:number;audit:number}>;rememberProviderCharge(providerIdempotencyKey:string,ref:string):Promise<void>;getProviderCharge(providerIdempotencyKey:string):Promise<string|null>;transaction<T>(fn:()=>Promise<T>):Promise<T>;}

export class MemoryInternalPaymentRepository implements InternalPaymentRepository{private locks=new Map<string,Promise<unknown>>();constructor(public store:PaymentStore=sharedMemoryPaymentStore,private failAfterProviderSuccess=false){}resetVolatileProcessState(){this.locks=new Map();}setFailAfterProviderSuccess(v:boolean){this.failAfterProviderSuccess=v;}async transaction<T>(fn:()=>Promise<T>){return fn();}private async lock<T>(key:string,fn:()=>Promise<T>):Promise<T>{const prev=this.locks.get(key)??Promise.resolve();let release!:()=>void;const next=new Promise<void>(r=>{release=r});this.locks.set(key,prev.then(()=>next));await prev;try{return await fn();}finally{release();if(this.locks.get(key)===next)this.locks.delete(key);}}async createOrReuseOperation(input:{subjectUserId:string;targetPlan:string;amount:number;currency:string;businessIdempotencyKey:string;provider:string}){const bk=businessKey(input.subjectUserId,input.businessIdempotencyKey);return this.lock(`op:${bk}`,async()=>{const existing=this.store.byBusiness.get(bk);if(existing)return {operation:this.store.operations.get(existing)!,reused:true};const now=new Date().toISOString();const op:InternalPaymentOperation={internalPaymentOperationId:id('ipo',bk),businessIdempotencyKey:bk,providerIdempotencyKey:id('pik',bk),subjectUserId:input.subjectUserId,targetPlan:input.targetPlan,amount:input.amount,currency:input.currency,provider:input.provider,state:'created',providerPaymentReference:null,providerCheckoutSessionReference:null,providerEventReferences:[],version:1,createdAt:now,updatedAt:now,expiresAt:new Date(Date.now()+30*60_000).toISOString(),reconciliationState:'not_required',lastProviderComparisonSnapshot:null,safeErrorCategory:null};this.store.operations.set(op.internalPaymentOperationId,op);this.store.byBusiness.set(bk,op.internalPaymentOperationId);this.store.byProviderKey.set(op.providerIdempotencyKey,op.internalPaymentOperationId);return {operation:op,reused:false};});}async getOperation(x:string){return this.store.operations.get(x)??null;}async findByProviderReference(r:string){return this.getOperation(this.store.byPaymentRef.get(r)??'');}async findBySessionReference(r:string){return this.getOperation(this.store.bySessionRef.get(r)??'');}async transition(idv:string,fromVersion:number,next:InternalPaymentState,patch:Partial<InternalPaymentOperation>={},message='transition'){return this.lock(`opid:${idv}`,async()=>{const op=this.store.operations.get(idv);if(!op)throw new Error('payment_operation_not_found');if(op.version!==fromVersion)return op;if(op.state!==next){if(!allowed[op.state].includes(next))throw new Error(`illegal_payment_transition:${op.state}->${next}`);op.state=next;op.version++;}Object.assign(op,patch);op.updatedAt=new Date().toISOString();this.store.audit.push({operationId:idv,message,createdAt:op.updatedAt,data:{state:op.state}});return op;});}async recordProviderAccepted(idv:string,paymentRef:string,sessionRef:string){if(this.store.byPaymentRef.has(paymentRef)&&this.store.byPaymentRef.get(paymentRef)!==idv)throw new Error('duplicate_provider_payment_reference');if(this.store.bySessionRef.has(sessionRef)&&this.store.bySessionRef.get(sessionRef)!==idv)throw new Error('duplicate_provider_checkout_session_reference');const op=this.store.operations.get(idv)!;if(this.failAfterProviderSuccess){op.reconciliationState='required';op.safeErrorCategory='repository_failure_after_provider_success';op.state='reconciliation_required';throw new Error('repository_failure_after_provider_success');}op.providerPaymentReference=paymentRef;op.providerCheckoutSessionReference=sessionRef;this.store.byPaymentRef.set(paymentRef,idv);this.store.bySessionRef.set(sessionRef,idv);return op;}async insertInbox(e:Inbox){return this.lock(`event:${e.eventId}`,async()=>{const old=this.store.inbox.get(e.eventId);if(old)return {inserted:false,event:old};this.store.inbox.set(e.eventId,e);return {inserted:true,event:e};});}async writeLedgerOnce(op:InternalPaymentOperation,kind:LedgerKind){const k=`${op.internalPaymentOperationId}:${kind}`;if(this.store.ledger.has(k))return false;this.store.ledger.set(k,{effectKey:k,operationId:op.internalPaymentOperationId,kind,amount:op.amount,currency:op.currency,createdAt:new Date().toISOString()});return true;}async writeEntitlementOnce(op:InternalPaymentOperation,kind:EntitlementKind){const k=`${op.internalPaymentOperationId}:${kind}`;if(this.store.entitlements.has(k))return false;this.store.entitlements.set(k,{effectKey:k,operationId:op.internalPaymentOperationId,kind,createdAt:new Date().toISOString()});return true;}async appendAudit(operationId:string|null,message:string,data:Record<string,unknown>){this.store.audit.push({operationId,message,data,createdAt:new Date().toISOString()});}async reconcileLookup(op:InternalPaymentOperation){const ref=this.store.providerCharges.get(op.providerIdempotencyKey)??null;return {providerPaymentReference:ref,providerChargeCount:ref?1:0};}async listUnknown(){return [...this.store.operations.values()].filter(o=>o.state==='unknown'||o.state==='reconciliation_required');}async counts(){return {operations:this.store.operations.size,providerCharges:this.store.providerCharges.size,ledger:this.store.ledger.size,entitlements:this.store.entitlements.size,inbox:this.store.inbox.size,audit:this.store.audit.length};}async rememberProviderCharge(k:string,r:string){this.store.providerCharges.set(k,r);}async getProviderCharge(k:string){return this.store.providerCharges.get(k)??null;}}

export class SQLInternalPaymentRepository implements InternalPaymentRepository{async transaction<T>(fn:()=>Promise<T>){return withDbTransaction(async()=>fn());}private row(x:any):InternalPaymentOperation{return {internalPaymentOperationId:x.internal_payment_operation_id,businessIdempotencyKey:x.business_idempotency_key,providerIdempotencyKey:x.provider_idempotency_key,subjectUserId:x.subject_user_id,targetPlan:x.target_plan,amount:Number(x.amount),currency:x.currency,provider:x.provider,state:x.state,providerPaymentReference:x.provider_payment_reference,providerCheckoutSessionReference:x.provider_checkout_session_reference,providerEventReferences:Array.isArray(x.provider_event_references)?x.provider_event_references:JSON.parse(x.provider_event_references??'[]'),version:Number(x.version),createdAt:x.created_at,updatedAt:x.updated_at,expiresAt:x.expires_at,reconciliationState:x.reconciliation_state,lastProviderComparisonSnapshot:typeof x.last_provider_comparison_snapshot==='string'?JSON.parse(x.last_provider_comparison_snapshot):x.last_provider_comparison_snapshot,safeErrorCategory:x.safe_error_category};}async createOrReuseOperation(input:{subjectUserId:string;targetPlan:string;amount:number;currency:string;businessIdempotencyKey:string;provider:string}){const bk=businessKey(input.subjectUserId,input.businessIdempotencyKey);const now=new Date().toISOString();const opId=id('ipo',bk);const pik=id('pik',bk);await queryDb(`INSERT INTO payment_operations (internal_payment_operation_id,business_idempotency_key,provider_idempotency_key,subject_user_id,target_plan,amount,currency,provider,state,provider_event_references,version,created_at,updated_at,expires_at,reconciliation_state) VALUES ($1,$2,$3,$4,$5,$6,$7,$8,'created','[]'::jsonb,1,$9,$9,$10,'not_required') ON CONFLICT (subject_user_id,business_idempotency_key) DO NOTHING`,[opId,bk,pik,input.subjectUserId,input.targetPlan,input.amount,input.currency,input.provider,now,new Date(Date.now()+30*60_000).toISOString()]);const rows=await queryDb<any>(`SELECT * FROM payment_operations WHERE subject_user_id=$1 AND business_idempotency_key=$2`,[input.subjectUserId,bk]);return {operation:this.row(rows[0]),reused:rows[0].internal_payment_operation_id!==opId?true:rows.length===1&&rows[0].created_at!==now};}async getOperation(idv:string){const r=await queryDb<any>(`SELECT * FROM payment_operations WHERE internal_payment_operation_id=$1`,[idv]);return r[0]?this.row(r[0]):null;}async findByProviderReference(ref:string){const r=await queryDb<any>(`SELECT * FROM payment_operations WHERE provider_payment_reference=$1`,[ref]);return r[0]?this.row(r[0]):null;}async findBySessionReference(ref:string){const r=await queryDb<any>(`SELECT * FROM payment_operations WHERE provider_checkout_session_reference=$1`,[ref]);return r[0]?this.row(r[0]):null;}async transition(idv:string,fromVersion:number,next:InternalPaymentState,patch:Partial<InternalPaymentOperation>={},message='transition'){const current=await this.getOperation(idv);if(!current)throw new Error('payment_operation_not_found');if(current.version!==fromVersion)return current;if(current.state!==next&&!allowed[current.state].includes(next))throw new Error(`illegal_payment_transition:${current.state}->${next}`);const updated={...current,...patch,state:next,version:current.state===next?current.version:current.version+1,updatedAt:new Date().toISOString()};await queryDb(`UPDATE payment_operations SET state=$2,version=$3,updated_at=$4,provider_payment_reference=$5,provider_checkout_session_reference=$6,provider_event_references=$7::jsonb,reconciliation_state=$8,last_provider_comparison_snapshot=$9::jsonb,safe_error_category=$10 WHERE internal_payment_operation_id=$1 AND version=$11`,[idv,updated.state,updated.version,updated.updatedAt,updated.providerPaymentReference,updated.providerCheckoutSessionReference,JSON.stringify(updated.providerEventReferences),updated.reconciliationState,JSON.stringify(updated.lastProviderComparisonSnapshot),updated.safeErrorCategory,fromVersion]);await this.appendAudit(idv,message,{state:updated.state});return (await this.getOperation(idv))!;}async recordProviderAccepted(idv:string,paymentRef:string,sessionRef:string){await queryDb(`UPDATE payment_operations SET provider_payment_reference=$2,provider_checkout_session_reference=$3,updated_at=$4 WHERE internal_payment_operation_id=$1`,[idv,paymentRef,sessionRef,new Date().toISOString()]);return (await this.getOperation(idv))!;}async insertInbox(e:Inbox){await queryDb(`INSERT INTO payment_provider_event_inbox (provider_event_id,provider,event_kind,provider_payment_reference,internal_payment_operation_id,payload,created_at) VALUES ($1,$2,$3,$4,$5,$6::jsonb,$7) ON CONFLICT (provider_event_id) DO NOTHING`,[e.eventId,e.provider,e.kind,e.providerPaymentReference,e.operationId,JSON.stringify(e.payload),e.createdAt]);const r=await queryDb<any>(`SELECT * FROM payment_provider_event_inbox WHERE provider_event_id=$1`,[e.eventId]);return {inserted:r[0]?.created_at===e.createdAt,event:{eventId:r[0].provider_event_id,provider:r[0].provider,kind:r[0].event_kind,providerPaymentReference:r[0].provider_payment_reference,operationId:r[0].internal_payment_operation_id,duplicate:false,createdAt:r[0].created_at,payload:typeof r[0].payload==='string'?JSON.parse(r[0].payload):r[0].payload}};}async writeLedgerOnce(op:InternalPaymentOperation,kind:LedgerKind){const k=`${op.internalPaymentOperationId}:${kind}`;await queryDb(`INSERT INTO payment_ledger_effects (effect_key,internal_payment_operation_id,effect_kind,amount,currency,created_at) VALUES ($1,$2,$3,$4,$5,$6) ON CONFLICT (effect_key) DO NOTHING`,[k,op.internalPaymentOperationId,kind,op.amount,op.currency,new Date().toISOString()]);return true;}async writeEntitlementOnce(op:InternalPaymentOperation,kind:EntitlementKind){const k=`${op.internalPaymentOperationId}:${kind}`;await queryDb(`INSERT INTO payment_entitlement_effects (effect_key,internal_payment_operation_id,subject_user_id,target_plan,effect_kind,created_at) VALUES ($1,$2,$3,$4,$5,$6) ON CONFLICT (effect_key) DO NOTHING`,[k,op.internalPaymentOperationId,op.subjectUserId,op.targetPlan,kind,new Date().toISOString()]);return true;}async appendAudit(operationId:string|null,message:string,data:Record<string,unknown>){await queryDb(`INSERT INTO payment_reconciliation_audit (audit_id,internal_payment_operation_id,message,audit_json,created_at) VALUES ($1,$2,$3,$4::jsonb,$5)`,[id('paudit',`${operationId}:${message}:${Date.now()}:${Math.random()}`),operationId,message,JSON.stringify(data),new Date().toISOString()]);}async reconcileLookup(op:InternalPaymentOperation){return {providerPaymentReference:op.providerPaymentReference,providerChargeCount:op.providerPaymentReference?1:0};}async listUnknown(){const r=await queryDb<any>(`SELECT * FROM payment_operations WHERE state IN ('unknown','reconciliation_required') ORDER BY updated_at DESC`);return r.map(x=>this.row(x));}async counts(){const [o,l,e,i,a]=await Promise.all([queryDb<any>(`SELECT count(*) c FROM payment_operations`),queryDb<any>(`SELECT count(*) c FROM payment_ledger_effects`),queryDb<any>(`SELECT count(*) c FROM payment_entitlement_effects`),queryDb<any>(`SELECT count(*) c FROM payment_provider_event_inbox`),queryDb<any>(`SELECT count(*) c FROM payment_reconciliation_audit`)]);return {operations:Number(o[0].c),providerCharges:0,ledger:Number(l[0].c),entitlements:Number(e[0].c),inbox:Number(i[0].c),audit:Number(a[0].c)};}async rememberProviderCharge(_k:string,_r:string){}async getProviderCharge(_k:string){return null;}}

export class InternalPaymentRuntime{public providerChargeAttempts=0;constructor(public mode:PaymentProviderMode='local_fake_provider',public repository:InternalPaymentRepository=createInternalPaymentRepository()){}resetVolatileProcessState(){if(this.repository instanceof MemoryInternalPaymentRepository)this.repository.resetVolatileProcessState();}async checkout(input:{subjectUserId:string;targetPlan:string;amount:number;currency:string;businessIdempotencyKey:string;outcome?:FakeProviderOutcome}){const created=await this.repository.createOrReuseOperation({...input,provider:'local_fake_provider'});if(created.reused)return {operation:created.operation,reused:true,providerMode:this.mode};if(this.mode==='disabled'){const op=await this.repository.transition(created.operation.internalPaymentOperationId,created.operation.version,'reconciliation_required',{safeErrorCategory:'provider_disabled',reconciliationState:'required'},'provider disabled');return {operation:op,reused:false,providerMode:this.mode};}try{return {operation:await this.invokeProvider(created.operation,input.outcome??'accepted'),reused:false,providerMode:this.mode};}catch(error){const fresh=await this.repository.getOperation(created.operation.internalPaymentOperationId);if(fresh&&(fresh.state==='unknown'||fresh.state==='reconciliation_required'))return {operation:fresh,reused:false,providerMode:this.mode};throw error;}}private async invokeProvider(op:InternalPaymentOperation,outcome:FakeProviderOutcome){if(terminal.has(op.state)||op.state==='unknown'||op.state==='reconciliation_required')return op;let next=await this.repository.transition(op.internalPaymentOperationId,op.version,'pending_provider',{},'provider call prepared');this.providerChargeAttempts++;const ref=await this.repository.getProviderCharge(next.providerIdempotencyKey)??id('pay',next.providerIdempotencyKey);if(outcome!=='provider_500_before_accepting'&&outcome!=='timeout_before_response')await this.repository.rememberProviderCharge(next.providerIdempotencyKey,ref);if(outcome==='accepted'||outcome==='duplicate_provider_reference'){next=await this.repository.recordProviderAccepted(next.internalPaymentOperationId,ref,id('chk',next.providerIdempotencyKey));return this.repository.transition(next.internalPaymentOperationId,next.version,'processing',{},'provider accepted');}return this.repository.transition(next.internalPaymentOperationId,next.version,outcome==='provider_500_before_accepting'?'failed':outcome==='timeout_before_response'?'unknown':'reconciliation_required',{safeErrorCategory:outcome,reconciliationState:'required',lastProviderComparisonSnapshot:{outcome,providerIdempotencyKey:next.providerIdempotencyKey,providerPaymentReference:await this.repository.getProviderCharge(next.providerIdempotencyKey)}},`provider ambiguous:${outcome}`);}async webhook(input:{eventId:string;kind:'success'|FakeProviderOutcome;providerPaymentReference?:string|null;providerCheckoutSessionReference?:string|null;operationId?:string|null;payload?:Record<string,unknown>}){return this.repository.transaction(async()=>{let op=input.operationId?await this.repository.getOperation(input.operationId):null;if(!op&&input.providerPaymentReference)op=await this.repository.findByProviderReference(input.providerPaymentReference);if(!op&&input.providerCheckoutSessionReference)op=await this.repository.findBySessionReference(input.providerCheckoutSessionReference);const stored=await this.repository.insertInbox({eventId:input.eventId,provider:'local_fake_provider',kind:input.kind,providerPaymentReference:input.providerPaymentReference??null,operationId:op?.internalPaymentOperationId??null,duplicate:false,createdAt:new Date().toISOString(),payload:input.payload??{}});if(!stored.inserted)return {duplicate:true,operation:stored.event.operationId?await this.repository.getOperation(stored.event.operationId):null};if(!op)return {duplicate:false,operation:null};if(input.providerPaymentReference&&!op.providerPaymentReference)op=await this.repository.recordProviderAccepted(op.internalPaymentOperationId,input.providerPaymentReference,input.providerCheckoutSessionReference??id('chk',op.providerIdempotencyKey));op={...op,providerEventReferences:[...new Set([...op.providerEventReferences,input.eventId])]};let next=op;if(input.kind==='success'||input.kind==='accepted'){next=await this.repository.transition(op.internalPaymentOperationId,op.version,'succeeded',{providerEventReferences:op.providerEventReferences},'provider success event');await this.effects(next,'payment_success');}else if(input.kind==='refund')next=await this.reversal(op,'refunded','refund');else if(input.kind==='partial_refund')next=await this.reversal(op,'partially_refunded','partial_refund');else if(input.kind==='reversal')next=await this.reversal(op,'reversed','reversal');else if(input.kind==='chargeback')next=await this.reversal(op,'chargeback','chargeback');else if(input.kind==='provider_500_before_accepting'&&op.state!=='succeeded')next=await this.repository.transition(op.internalPaymentOperationId,op.version,'failed',{providerEventReferences:op.providerEventReferences},'provider failure event');return {duplicate:false,operation:next};});}private async effects(op:InternalPaymentOperation,kind:LedgerKind){await this.repository.writeLedgerOnce(op,kind);await this.repository.writeEntitlementOnce(op,kind==='payment_success'?'grant':kind);}private async reversal(op:InternalPaymentOperation,state:InternalPaymentState,kind:LedgerKind){const next=await this.repository.transition(op.internalPaymentOperationId,op.version,state,{providerEventReferences:op.providerEventReferences},kind);await this.effects(next,kind);return next;}async reconcile(input:{operationId?:string;businessIdempotencyKey?:string;subjectUserId?:string}){return this.repository.transaction(async()=>{const op=input.operationId?await this.repository.getOperation(input.operationId):null;if(!op)return {status:'not_found' as const};const snapshot=await this.repository.reconcileLookup(op);let next=op;if(snapshot.providerPaymentReference&&(op.state==='unknown'||op.state==='reconciliation_required')){next=await this.repository.transition(op.internalPaymentOperationId,op.version,'succeeded',{providerPaymentReference:snapshot.providerPaymentReference,reconciliationState:'matched',lastProviderComparisonSnapshot:snapshot},'reconciliation matched provider success');await this.effects(next,'payment_success');}else if(!snapshot.providerPaymentReference&&(op.state==='unknown'||op.state==='reconciliation_required'))next=await this.repository.transition(op.internalPaymentOperationId,op.version,op.state,{reconciliationState:'mismatch',lastProviderComparisonSnapshot:snapshot},'reconciliation mismatch');await this.repository.appendAudit(op.internalPaymentOperationId,'operator reconciliation inspected',snapshot);return {status:'ok' as const,operation:next,duplicateChargeRisk:snapshot.providerChargeCount>1,localProviderMismatch:next.reconciliationState==='mismatch'};});}counts(){return this.repository.counts();}listUnknown(){return this.repository.listUnknown();}}
export function createInternalPaymentRepository():InternalPaymentRepository{return env().APP_STATE_REPOSITORY==='sql'&&!!env().DATABASE_URL?new SQLInternalPaymentRepository():new MemoryInternalPaymentRepository();}
export const internalPaymentRuntime=new InternalPaymentRuntime((env().ELCEO_PAYMENT_PROVIDER_MODE as PaymentProviderMode)||'local_fake_provider');
