{
  "name": "@elceo/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --check src"
  },
  "dependencies": {
    "@elceo/config": "0.1.0",
    "@elceo/providers": "0.1.0",
    "@elceo/schemas": "0.1.0",
    "@elceo/types": "0.1.0"
  }
}
