{
  "name": "@elceo/reasoning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --check src",
    "test": "tsc -p tsconfig.test.json && node -e \"require('node:fs').writeFileSync('dist-test/package.json', JSON.stringify({type:'commonjs'}))\" && node dist-test/services/reasoning/src/tests/run-tests.js"
  },
  "dependencies": {
    "@elceo/config": "0.1.0",
    "@elceo/domain": "0.1.0",
    "@elceo/schemas": "0.1.0",
    "@elceo/types": "0.1.0",
    "@elceo/ingestion": "0.1.0"
  }
}
