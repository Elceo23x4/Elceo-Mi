import { canonicalJson, deepCloneFreeze } from '../expectation-reality/identity';
import type { FragilityScoreEvaluation } from './contracts';
export type FragilityScoreRepository={saveEvaluation(e:FragilityScoreEvaluation):Promise<FragilityScoreEvaluation>;getEvaluationById(id:string):Promise<FragilityScoreEvaluation|null>;listEvaluations(params?:{from?:string;to?:string}):Promise<FragilityScoreEvaluation[]>};
export class MemoryFragilityScoreRepository implements FragilityScoreRepository{private rows=new Map<string,FragilityScoreEvaluation>();async saveEvaluation(e:FragilityScoreEvaluation){const prior=this.rows.get(e.fragilityScoreEvaluationId);if(prior){if(canonicalJson(prior)!==canonicalJson(e))throw new Error('immutable_fragility_evaluation_conflict');return prior;}const frozen=deepCloneFreeze(e);this.rows.set(e.fragilityScoreEvaluationId,frozen);return frozen;}async getEvaluationById(id:string){return this.rows.get(id)??null;}async listEvaluations(p:{from?:string;to?:string}={}){return [...this.rows.values()].filter(x=>(!p.from||x.createdAt>=p.from)&&(!p.to||x.createdAt<=p.to)).sort((a,b)=>a.createdAt.localeCompare(b.createdAt)||a.fragilityScoreEvaluationId.localeCompare(b.fragilityScoreEvaluationId));}}
