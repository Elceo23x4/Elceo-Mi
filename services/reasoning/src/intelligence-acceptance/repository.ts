import {canonicalJson,deepCloneFreeze} from '../expectation-reality/identity';import type {AcceptanceRecord,CalibrationTrial,ConfigurationVersion,DatasetManifest} from './contracts';
export type AcceptanceEntity=DatasetManifest|ConfigurationVersion|CalibrationTrial|AcceptanceRecord;
export interface IntelligenceAcceptanceRepository{save(kind:string,id:string,value:AcceptanceEntity):Promise<AcceptanceEntity>;get(kind:string,id:string):Promise<AcceptanceEntity|null>}
export class MemoryIntelligenceAcceptanceRepository implements IntelligenceAcceptanceRepository{private rows=new Map<string,AcceptanceEntity>();async save(kind:string,id:string,value:AcceptanceEntity){const key=`${kind}:${id}`,p=this.rows.get(key);if(p&&canonicalJson(p)!==canonicalJson(value))throw new Error('immutable_acceptance_conflict');if(p)return p;const frozen=deepCloneFreeze(value);this.rows.set(key,frozen);return frozen;}async get(kind:string,id:string){return this.rows.get(`${kind}:${id}`)??null;}}
