import { canonicalJson } from '../expectation-reality/identity';
import type { MarketCleanlinessEvaluation } from './contracts';
import { immutableCleanliness } from './identity';
export type MarketCleanlinessRepository={saveEvaluation(record:MarketCleanlinessEvaluation):Promise<MarketCleanlinessEvaluation>;getEvaluationById(id:string):Promise<MarketCleanlinessEvaluation|null>;listEvaluations(params?:{from?:string;to?:string}):Promise<MarketCleanlinessEvaluation[]>};
export class MemoryMarketCleanlinessRepository implements MarketCleanlinessRepository {private rows=new Map<string,MarketCleanlinessEvaluation>();async saveEvaluation(record:MarketCleanlinessEvaluation){const prior=this.rows.get(record.cleanlinessEvaluationId);if(prior){if(canonicalJson(prior)!==canonicalJson(record))throw new Error('immutable_cleanliness_evaluation_conflict');return prior;}const frozen=immutableCleanliness(record);this.rows.set(record.cleanlinessEvaluationId,frozen);return frozen;}async getEvaluationById(id:string){return this.rows.get(id)??null;}async listEvaluations(params:{from?:string;to?:string}={}){return [...this.rows.values()].filter(r=>(!params.from||r.createdAt>=params.from)&&(!params.to||r.createdAt<=params.to)).sort((a,b)=>a.createdAt.localeCompare(b.createdAt)||a.cleanlinessEvaluationId.localeCompare(b.cleanlinessEvaluationId));}}
