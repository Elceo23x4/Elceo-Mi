import type { PositioningStressEvaluation } from './contracts';
export function buildPositioningStressReport(rows:readonly PositioningStressEvaluation[],generatedAt:string){const count=(key:keyof PositioningStressEvaluation)=>Object.fromEntries([...new Set(rows.map(r=>String(r[key])))].sort().map(v=>[v,rows.filter(r=>String(r[key])===v).length]));return{generatedAt,sampleSize:rows.length,stateCounts:count('marketStressState'),positioningAvailability:count('positioningEvidenceState'),crowdPainQualification:count('crowdPainQualification'),crowdingStateCounts:count('crowdingState'),pressuredSideCounts:count('pressuredSide'),insufficientHistoryCount:rows.filter(r=>r.positioningEvidenceState==='insufficient_history').length,provenanceLimitationCount:rows.filter(r=>r.positioningEvidenceState==='provenance_limited'||r.marketStressEvidenceSufficiency==='provenance_limited').length,periodStart:rows.map(r=>r.createdAt).sort()[0]??null,periodEnd:rows.map(r=>r.createdAt).sort().at(-1)??null,warnings:[],limitations:['Descriptive distribution only; no predictive or profitability claim.']};}
