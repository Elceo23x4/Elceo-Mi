import { canonicalJson } from '../expectation-reality/identity';import type { PositioningStressEvaluation } from './contracts';import { immutablePositioning } from './identity';
export type PositioningStressRepository={saveEvaluation(r:PositioningStressEvaluation):Promise<PositioningStressEvaluation>;getEvaluationById(id:string):Promise<PositioningStressEvaluation|null>;listEvaluations(params?:{from?:string;to?:string}):Promise<PositioningStressEvaluation[]>};
export class MemoryPositioningStressRepository implements PositioningStressRepository{private rows=new Map<string,PositioningStressEvaluation>();async saveEvaluation(r:PositioningStressEvaluation){const p=this.rows.get(r.positioningStressEvaluationId);if(p){if(canonicalJson(p)!==canonicalJson(r))throw new Error('immutable_positioning_stress_conflict');return p;}const v=immutablePositioning(r);this.rows.set(r.positioningStressEvaluationId,v);return v;}async getEvaluationById(id:string){return this.rows.get(id)??null;}async listEvaluations(p:{from?:string;to?:string}={}){return [...this.rows.values()].filter(x=>(!p.from||x.createdAt>=p.from)&&(!p.to||x.createdAt<=p.to));}}
