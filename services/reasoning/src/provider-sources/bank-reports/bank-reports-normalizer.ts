import type { NormalizedBankEarningsMetric, NormalizedBankHealthMetric, NormalizedMarketEvidencePayload } from '@elceo/types';
import { buildMetadataJson, buildNormalizedPayloadId, buildValuesJson, clampConfidenceScore } from '../normalization-helpers';
import type { BankEarningsMetricRow, BankHealthMetricRow, BankReportsFixtureRequest } from './bank-reports-contracts';
const finite=(n:number)=>{if(!Number.isFinite(n)) throw new Error('bank_reports_invalid_finite_number');};
const dq=(row:Record<string,string>)=>Object.values(row).every(Boolean)?'high':'partial';
export const buildBankHealthPayload=(r:BankReportsFixtureRequest,row:BankHealthMetricRow):NormalizedMarketEvidencePayload=>{finite(row.value); const health:NormalizedBankHealthMetric={...row,reportDate:new Date(row.reportDate).toISOString(),providerId:r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'bank_health',health.reportDate,health.institution),evidenceTypeId:'bank_health',evidenceClass:'bank_health',providerId:r.providerId,sourceId:health.metricName,region:health.region,asset:null,observedAt:health.reportDate,publishedAt:health.reportDate,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(95),dataQuality:dq({institution:row.institution,region:row.region,metricName:row.metricName,unit:row.unit}),valuesJson:buildValuesJson(health),metadataJson:buildMetadataJson({capability:r.capability})};};
export const buildBankEarningsPayload=(r:BankReportsFixtureRequest,row:BankEarningsMetricRow):NormalizedMarketEvidencePayload=>{finite(row.value); const earnings:NormalizedBankEarningsMetric={...row,reportDate:new Date(row.reportDate).toISOString(),providerId:r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'bank_earnings',earnings.reportDate,`${earnings.institution}|${earnings.fiscalPeriod}`),evidenceTypeId:'bank_earnings',evidenceClass:'bank_earnings',providerId:r.providerId,sourceId:earnings.metricName,region:earnings.region,asset:null,observedAt:earnings.reportDate,publishedAt:earnings.reportDate,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(94),dataQuality:dq({institution:row.institution,region:row.region,metricName:row.metricName,fiscalPeriod:row.fiscalPeriod,unit:row.unit}),valuesJson:buildValuesJson(earnings),metadataJson:buildMetadataJson({capability:r.capability})};};
