import type { NormalizedEnergyCommodityPoint, NormalizedMarketEvidencePayload, NormalizedPreciousMetalsFlowPoint } from '@elceo/types';
import { buildMetadataJson, buildNormalizedPayloadId, buildValuesJson, clampConfidenceScore } from '../normalization-helpers';
import type { CommoditiesMetalsFixtureRequest, EnergyCommodityRow, PreciousMetalsFlowRow } from './commodities-metals-contracts';
const finite=(n:number)=>{if(!Number.isFinite(n)) throw new Error('commodities_metals_invalid_finite_number');};
const dq=(v:string[])=>v.every((x)=>x.length>0)?'high':'partial';
export const buildEnergyCommodityPayload=(r:CommoditiesMetalsFixtureRequest,row:EnergyCommodityRow):NormalizedMarketEvidencePayload=>{finite(row.value); const p:NormalizedEnergyCommodityPoint={commodity:row.commodityName,observedAt:new Date(row.observedAt).toISOString(),price:row.value,currency:row.unit.split('_')[0]??'usd',providerId:row.providerId??r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'energy_commodities',p.observedAt,`${row.asset}|${row.region}`),evidenceTypeId:'energy_commodities',evidenceClass:'energy_commodities',providerId:r.providerId,sourceId:row.commodityName,region:row.region,asset:row.asset,observedAt:p.observedAt,publishedAt:p.observedAt,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(95),dataQuality:dq([row.asset,row.region,row.commodityName,row.unit]),valuesJson:buildValuesJson(p),metadataJson:buildMetadataJson({capability:r.capability})};};
export const buildPreciousMetalsFlowPayload=(r:CommoditiesMetalsFixtureRequest,row:PreciousMetalsFlowRow):NormalizedMarketEvidencePayload=>{finite(row.value); const p:NormalizedPreciousMetalsFlowPoint={asset:row.asset,observedAt:new Date(row.observedAt).toISOString(),flowAmount:row.value,unit:row.unit,providerId:row.providerId??r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'precious_metals_flows',p.observedAt,`${row.asset}|${row.flowType}`),evidenceTypeId:'precious_metals_flows',evidenceClass:'precious_metals_flows',providerId:r.providerId,sourceId:row.flowType,region:row.region,asset:row.asset,observedAt:p.observedAt,publishedAt:p.observedAt,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(94),dataQuality:dq([row.asset,row.region,row.flowType,row.unit]),valuesJson:buildValuesJson(p),metadataJson:buildMetadataJson({capability:r.capability})};};
