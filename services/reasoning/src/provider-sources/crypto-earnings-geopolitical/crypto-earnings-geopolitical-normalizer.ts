import type { NormalizedCryptoMarketStructurePoint, NormalizedEarningsMacroPoint, NormalizedGeopoliticalRiskEvent, NormalizedMarketEvidencePayload } from '@elceo/types';
import { buildMetadataJson, buildNormalizedPayloadId, buildValuesJson, clampConfidenceScore } from '../normalization-helpers';
import type { CryptoEarningsGeopoliticalFixtureRequest, CryptoMarketStructureRow, EarningsMacroRow, GeopoliticalRiskEventRow } from './crypto-earnings-geopolitical-contracts';
const finite=(n:number)=>{if(!Number.isFinite(n)) throw new Error('crypto_earnings_geopolitical_invalid_finite_number');};
const isHttp=(v:string)=>v.startsWith('http://')||v.startsWith('https://');
export const buildCryptoPayload=(r:CryptoEarningsGeopoliticalFixtureRequest,row:CryptoMarketStructureRow):NormalizedMarketEvidencePayload=>{finite(row.value); const p:NormalizedCryptoMarketStructurePoint={asset:row.asset,observedAt:new Date(row.observedAt).toISOString(),metricName:row.indicatorName,value:row.value,unit:row.unit,providerId:row.providerId??r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'crypto_market_structure',p.observedAt,`${row.asset}|${row.indicatorName}`),evidenceTypeId:'crypto_market_structure',evidenceClass:'crypto_market_structure',providerId:r.providerId,sourceId:row.indicatorName,region:r.region??'global',asset:row.asset,observedAt:p.observedAt,publishedAt:p.observedAt,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(94),dataQuality:'high',valuesJson:buildValuesJson(p),metadataJson:buildMetadataJson({capability:r.capability})};};
export const buildEarningsPayload=(r:CryptoEarningsGeopoliticalFixtureRequest,row:EarningsMacroRow):NormalizedMarketEvidencePayload=>{finite(row.value); const p:NormalizedEarningsMacroPoint={region:row.region,observedAt:new Date(row.reportDate).toISOString(),metricName:row.metricName,value:row.value,unit:row.unit,providerId:row.providerId??r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'earnings_macro',p.observedAt,`${row.institution}|${row.metricName}`),evidenceTypeId:'earnings_macro',evidenceClass:'earnings_macro',providerId:r.providerId,sourceId:row.institution,region:row.region,asset:null,observedAt:p.observedAt,publishedAt:p.observedAt,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(93),dataQuality:'high',valuesJson:buildValuesJson(p),metadataJson:buildMetadataJson({capability:r.capability})};};
export const buildGeopoliticalPayload=(r:CryptoEarningsGeopoliticalFixtureRequest,row:GeopoliticalRiskEventRow):NormalizedMarketEvidencePayload=>{finite(row.importanceScore); if(row.sourceUrl!==null&&!isHttp(row.sourceUrl)) throw new Error('crypto_earnings_geopolitical_invalid_source_url'); const p:NormalizedGeopoliticalRiskEvent={eventId:row.eventId,region:row.region,publishedAt:new Date(row.occurredAt).toISOString(),severity:row.severity,title:row.title,importanceScore:row.importanceScore,relatedAssets:row.relatedAssets,sourceUrl:row.sourceUrl,providerId:row.providerId??r.providerId}; return {payloadId:buildNormalizedPayloadId(r.providerId,'geopolitical_risk',p.publishedAt,row.eventId),evidenceTypeId:'geopolitical_risk',evidenceClass:'geopolitical_risk',providerId:r.providerId,sourceId:row.eventId,region:row.region,asset:row.relatedAssets[0]??null,observedAt:p.publishedAt,publishedAt:p.publishedAt,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(90),dataQuality:row.sourceUrl?'high':'medium',valuesJson:buildValuesJson(p),metadataJson:buildMetadataJson({capability:r.capability})};};
