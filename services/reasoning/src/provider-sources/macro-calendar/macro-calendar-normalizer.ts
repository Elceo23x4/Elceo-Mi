import type { NormalizedMacroCalendarEvent, NormalizedMarketEvidencePayload } from '@elceo/types';
import { buildMetadataJson, buildNormalizedPayloadId, buildValuesJson, clampConfidenceScore } from '../normalization-helpers';
import type { MacroCalendarEventRow, MacroCalendarFixtureRequest } from './macro-calendar-contracts';
export const buildMacroCalendarPayload=(r:MacroCalendarFixtureRequest,row:MacroCalendarEventRow):NormalizedMarketEvidencePayload=>{if(!Number.isFinite(row.importanceScore)||row.importanceScore<0||row.importanceScore>100) throw new Error('macro_calendar_invalid_importance_score'); const event:NormalizedMacroCalendarEvent={...row,scheduledFor:new Date(row.scheduledFor).toISOString(),providerId:r.providerId}; const complete=event.actual!==null&&event.consensus!==null&&event.previous!==null; return {payloadId:buildNormalizedPayloadId(r.providerId,'macro_calendar_events',event.scheduledFor,event.countryOrBloc),evidenceTypeId:'macro_calendar_events',evidenceClass:'macro_calendar',providerId:r.providerId,sourceId:event.eventId,region:event.region,asset:null,observedAt:event.scheduledFor,publishedAt:null,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(complete?95:86),dataQuality:complete?'high':(event.consensus!==null?'medium':'partial'),valuesJson:buildValuesJson(event),metadataJson:buildMetadataJson({family:'macro_calendar',category:event.eventCategory})};};
