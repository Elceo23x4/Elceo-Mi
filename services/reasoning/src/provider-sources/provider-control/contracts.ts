export type ProviderControlPolicyVersion = string;
export type ProviderControlScope = { sourceId:string; capabilityId:string; credentialPoolId:string; policyVersion:ProviderControlPolicyVersion };
export type ProviderWindowPolicy = { kind:'fixed_duration'; windowMs:number };
export type ProviderRatePolicy = { capacity:number; refillAmount:number; refillIntervalMs:number; requestTokens:number };
export type ProviderQuotaPolicy = ProviderWindowPolicy & { limit:number };
export type ProviderCostPolicy = ProviderWindowPolicy & { budgetUnits:number; requestCostUnits:number };
export type ProviderConcurrencyPolicy = { maxConcurrent:number; leaseDurationMs:number; providerTimeoutMs:number };
export type ProviderControlPolicy = ProviderControlScope & { policyId:string; status:'approved'|'test_only'|'disabled'; effectiveFrom:string; effectiveTo:string|null; provenance:string; canonicalPolicyHash:string; rate:ProviderRatePolicy; quota?:ProviderQuotaPolicy; cost:ProviderCostPolicy; concurrency:ProviderConcurrencyPolicy };
export type ProviderReservationStatus = 'RESERVED'|'EXECUTING'|'COMMITTED'|'RELEASED'|'COMMIT_REQUIRED_UNKNOWN_OUTCOME';
export type ProviderSettlementState = 'not_required'|'reserved'|'settled_committed'|'settled_released'|'settled_unknown_outcome'|'settlement_unconfirmed';
export type ProviderAdmissionRequest = { admissionId:string; reservationId:string; ownerToken:string; requestId:string; fingerprint:string; policy:ProviderControlPolicy };
export type ProviderControlSnapshot = { scope:ProviderControlScope; rate:{capacity:number;remaining:number;retryAfterMs:number}; quota:null|{limit:number;used:number;remaining:number;windowStart:number;windowEnd:number;resetAt:number}; cost:{limit:number;reserved:number;committed:number;remaining:number;windowStart:number;windowEnd:number}; concurrency:{limit:number;active:number;leaseExpiresAt:number} };
export type ProviderReservation = { admissionId:string; reservationId:string; requestId:string; fingerprint:string; policyHash:string; ownerToken:string; executionToken?:string; executionLeaseExpiresAt?:number; claimedAt?:number; leaseDurationMs:number; status:ProviderReservationStatus; costUnits:number; rateTokens:number; quotaUnits:number; quotaWindowStart:number; costWindowStart:number; snapshot:ProviderControlSnapshot };
export type ProviderAdmissionDenialReason = 'provider_rate_exhausted'|'provider_quota_exhausted'|'provider_cost_exhausted'|'provider_concurrency_exhausted'|'provider_control_idempotency_conflict'|'provider_control_admission_already_settled'|'provider_control_admission_in_progress'|'provider_control_lease_expired'|'provider_control_unavailable';
export type ProviderAdmissionDecision = { allowed:true; reason:'provider_control_admitted'; reused:boolean; reservation:ProviderReservation } | { allowed:false; reason:ProviderAdmissionDenialReason; retryAfterMs:number; snapshot?:ProviderControlSnapshot };
export type ProviderExecutionClaim = { claimed:true; reservation:ProviderReservation }|{ claimed:false; reason:'provider_control_admission_in_progress'|'provider_control_lease_expired'|'provider_control_admission_already_settled'|'provider_control_unavailable' };
export interface ProviderControlStore { readonly kind:'memory'|'redis'; isReady():boolean; admit(request:ProviderAdmissionRequest):Promise<ProviderAdmissionDecision>; claimExecution(reservation:ProviderReservation,executionToken:string):Promise<ProviderExecutionClaim>; settle(reservation:ProviderReservation,status:Exclude<ProviderReservationStatus,'RESERVED'|'EXECUTING'>):Promise<boolean>; close():Promise<void> }
export type ProviderControlPolicyResolutionRequest = { sourceId:string; capabilityId:string; credentialPoolId:string; evaluatedAt:number };
export interface ProviderControlPolicyResolver { resolve(request:ProviderControlPolicyResolutionRequest):Promise<ProviderControlPolicy|null> }
