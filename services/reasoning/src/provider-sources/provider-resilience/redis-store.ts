import {createClient,type RedisClientType}from'redis';import type{ProviderProbeLease,ProviderResilienceOutcome,ProviderResiliencePermission,ProviderResiliencePolicy,ProviderResilienceSnapshot,ProviderResilienceStore}from'./contracts';import{providerResilienceKeys}from'./keys';import{validateProviderResiliencePolicy}from'./policy';
const ACQUIRE=`local t=redis.call('TIME');local now=tonumber(t[1])*1000+math.floor(tonumber(t[2])/1000);redis.call('ZREMRANGEBYSCORE',KEYS[2],'-inf',now);local state=redis.call('HGET',KEYS[1],'state') or 'CLOSED';local gen=tonumber(redis.call('HGET',KEYS[1],'generation') or 0);local untilAt=tonumber(redis.call('HGET',KEYS[1],'openUntil') or 0);if state=='OPEN' and now>=untilAt then state='HALF_OPEN';untilAt=0;redis.call('HSET',KEYS[1],'state',state);redis.call('HDEL',KEYS[1],'openUntil') end;local active=redis.call('ZCARD',KEYS[2]);local allowed=false;local reason='provider_resilience_open';local expiry=0;if state=='CLOSED' then allowed=true;reason='provider_resilience_closed' elseif state=='HALF_OPEN' then if active<tonumber(ARGV[1]) then expiry=now+tonumber(ARGV[2]);redis.call('ZADD',KEYS[2],expiry,ARGV[3]);active=active+1;allowed=true;reason='provider_resilience_probe' else reason='provider_resilience_probe_limit' end end;redis.call('PEXPIRE',KEYS[1],ARGV[4]);redis.call('PEXPIRE',KEYS[2],ARGV[4]);return cjson.encode({allowed=allowed,reason=reason,state=state,generation=gen,failures=tonumber(redis.call('HGET',KEYS[1],'failures') or 0),observations=tonumber(redis.call('HGET',KEYS[1],'observations') or 0),windowStart=tonumber(redis.call('HGET',KEYS[1],'windowStart') or 0),openUntil=untilAt,active=active,expiry=expiry,classification=redis.call('HGET',KEYS[1],'classification')})`;
const OBSERVE=`local t=redis.call('TIME');local now=tonumber(t[1])*1000+math.floor(tonumber(t[2])/1000);local state=redis.call('HGET',KEYS[1],'state') or 'CLOSED';local gen=tonumber(redis.call('HGET',KEYS[1],'generation') or 0);local isProbe=ARGV[9]~='';local class=ARGV[1];local excluded=class=='not_eligible' or (class~='success' and ARGV[2]~='1');if excluded and not isProbe then return cjson.encode({applied=false,reason='provider_resilience_observation_excluded',state=state,generation=gen,failures=tonumber(redis.call('HGET',KEYS[1],'failures') or 0),observations=tonumber(redis.call('HGET',KEYS[1],'observations') or 0),windowStart=tonumber(redis.call('HGET',KEYS[1],'windowStart') or 0),openUntil=tonumber(redis.call('HGET',KEYS[1],'openUntil') or 0),active=redis.call('ZCARD',KEYS[2]),classification=redis.call('HGET',KEYS[1],'classification')}) end;if isProbe then local score=redis.call('ZSCORE',KEYS[2],ARGV[9]);if state~='HALF_OPEN' or gen~=tonumber(ARGV[10]) or not score or tonumber(score)~=tonumber(ARGV[11]) then return cjson.encode({applied=false,reason='provider_resilience_stale_probe',state=state,generation=gen}) end;if tonumber(score)<=now then redis.call('ZREM',KEYS[2],ARGV[9]);return cjson.encode({applied=false,reason='provider_resilience_expired_probe',state=state,generation=gen}) end;redis.call('ZREM',KEYS[2],ARGV[9]) end;if excluded then return cjson.encode({applied=false,reason='provider_resilience_observation_excluded',state=state,generation=gen,failures=tonumber(redis.call('HGET',KEYS[1],'failures') or 0),observations=tonumber(redis.call('HGET',KEYS[1],'observations') or 0),windowStart=tonumber(redis.call('HGET',KEYS[1],'windowStart') or 0),openUntil=tonumber(redis.call('HGET',KEYS[1],'openUntil') or 0),active=redis.call('ZCARD',KEYS[2]),classification=redis.call('HGET',KEYS[1],'classification')}) end;redis.call('HSET',KEYS[1],'classification',class);if class=='success' then if isProbe then state='CLOSED';gen=gen+1;redis.call('DEL',KEYS[2]);redis.call('HSET',KEYS[1],'state',state,'generation',gen,'failures',0,'observations',0);redis.call('HDEL',KEYS[1],'windowStart','openUntil');redis.call('PEXPIRE',KEYS[1],ARGV[8]);return cjson.encode({applied=true,reason='provider_resilience_success',state=state,generation=gen,failures=0,observations=0}) end;local failures=tonumber(redis.call('HGET',KEYS[1],'failures') or 0);local observations=tonumber(redis.call('HGET',KEYS[1],'observations') or 0);local ws=tonumber(redis.call('HGET',KEYS[1],'windowStart') or 0);if ws==0 or now-ws>=tonumber(ARGV[3]) then ws=now;failures=0;observations=0 end;observations=observations+1;if failures>=tonumber(ARGV[4]) and observations>=tonumber(ARGV[5]) then state='OPEN';gen=gen+1;local untilAt=now+tonumber(ARGV[6]);redis.call('HSET',KEYS[1],'state',state,'generation',gen,'openUntil',untilAt,'failures',failures,'observations',observations,'windowStart',ws,'classification',class) else redis.call('HSET',KEYS[1],'failures',failures,'observations',observations,'windowStart',ws,'classification',class) end;redis.call('PEXPIRE',KEYS[1],ARGV[8]);return cjson.encode({applied=true,reason='provider_resilience_success',state=state,generation=gen,failures=failures,observations=observations,windowStart=ws}) end;local failures=tonumber(redis.call('HGET',KEYS[1],'failures') or 0);local observations=tonumber(redis.call('HGET',KEYS[1],'observations') or 0);local ws=tonumber(redis.call('HGET',KEYS[1],'windowStart') or 0);if ws==0 or now-ws>=tonumber(ARGV[3]) then ws=now;failures=0;observations=0 end;failures=failures+1;observations=observations+1;local retry=tonumber(ARGV[7]);if isProbe or retry>=0 or (failures>=tonumber(ARGV[4]) and observations>=tonumber(ARGV[5])) then state='OPEN';gen=gen+1;local duration=tonumber(ARGV[6]);if retry>duration then duration=retry end;local untilAt=now+duration;redis.call('DEL',KEYS[2]);redis.call('HSET',KEYS[1],'state',state,'generation',gen,'openUntil',untilAt,'failures',failures,'observations',observations,'windowStart',ws,'classification',class);redis.call('PEXPIRE',KEYS[1],ARGV[8]);return cjson.encode({applied=true,reason=isProbe and 'provider_resilience_reopened' or 'provider_resilience_opened',state=state,generation=gen,openUntil=untilAt,failures=failures,observations=observations,windowStart=ws}) end;redis.call('HSET',KEYS[1],'failures',failures,'observations',observations,'windowStart',ws);redis.call('PEXPIRE',KEYS[1],ARGV[8]);return cjson.encode({applied=true,reason='provider_resilience_failure_recorded',state=state,generation=gen,failures=failures,observations=observations,windowStart=ws})`;
const RELEASE=`local t=redis.call('TIME');local now=tonumber(t[1])*1000+math.floor(tonumber(t[2])/1000);local state=redis.call('HGET',KEYS[1],'state') or 'CLOSED';local gen=tonumber(redis.call('HGET',KEYS[1],'generation') or 0);if state~='HALF_OPEN' or gen~=tonumber(ARGV[2]) then return 0 end;local score=redis.call('ZSCORE',KEYS[2],ARGV[1]);if not score or tonumber(score)~=tonumber(ARGV[3]) then return 0 end;redis.call('ZREM',KEYS[2],ARGV[1]);redis.call('PEXPIRE',KEYS[1],ARGV[4]);redis.call('PEXPIRE',KEYS[2],ARGV[4]);return 1`;
type Raw={allowed?:boolean;applied?:boolean;reason:string;state?:'CLOSED'|'OPEN'|'HALF_OPEN';generation?:number;failures?:number;observations?:number;windowStart?:number;openUntil?:number;active?:number;expiry?:number;classification?:ProviderResilienceOutcome['classification']|false};
export function createProviderResilienceRedisClient(options:{url?:string;connectTimeoutMs?:number}={}):RedisClientType{const url=options.url??process.env.REDIS_URL;if(!url)throw new Error('provider_resilience_unavailable');const client=createClient({url,socket:{connectTimeout:options.connectTimeoutMs??3000,reconnectStrategy:r=>r<3?Math.min(100*2**r,1000):false}})as RedisClientType;client.on('error',()=>undefined);return client;}
export class RedisProviderResilienceStore implements ProviderResilienceStore{readonly kind='redis'as const;private ready=false;private connecting:Promise<unknown>|null=null;constructor(private client:RedisClientType,private namespace='elceo:provider-resilience:v1',private timeoutMs=3000){}isReady(){return this.ready&&this.client.isReady;}private async bounded<T>(p:Promise<T>){let timer:NodeJS.Timeout;try{return await Promise.race([p,new Promise<T>((_,r)=>{timer=setTimeout(()=>r(new Error('timeout')),this.timeoutMs)})]);}finally{clearTimeout(timer!);}}private async ensure(){if(this.connecting)await this.connecting;if(!this.client.isOpen){this.connecting=this.bounded(this.client.connect()).finally(()=>this.connecting=null);await this.connecting;}this.ready=this.client.isReady;if(!this.ready)throw new Error('provider_resilience_unavailable');}private snap(p:ProviderResiliencePolicy,r:Raw):ProviderResilienceSnapshot{return{policyId:p.policyId,policyVersion:p.policyVersion,policyHash:p.canonicalPolicyHash,state:r.state??'CLOSED',generation:r.generation??0,failureCount:r.failures??0,observationCount:r.observations??0,windowStartedAt:r.windowStart||null,openUntil:r.openUntil||null,probeActive:r.active??0,probeLeaseExpiresAt:r.expiry||null,classification:r.classification||null,reason:r.reason};}
 async acquire(p:ProviderResiliencePolicy,owner:string):Promise<ProviderResiliencePermission>{try{validateProviderResiliencePolicy(p);await this.ensure();const k=providerResilienceKeys(p,this.namespace),r=JSON.parse(String(await this.bounded(this.client.eval(ACQUIRE,{keys:[k.state,k.probes],arguments:[p.halfOpenMaxConcurrent,p.probeLeaseMs,owner,p.stateTtlMs].map(String)}))))as Raw,s=this.snap(p,r);if(!r.allowed)return{allowed:false,reason:r.reason as 'provider_resilience_open'|'provider_resilience_probe_limit',snapshot:s};return{allowed:true,reason:r.reason as 'provider_resilience_closed'|'provider_resilience_probe',...(r.expiry?{probe:{ownerToken:owner,generation:r.generation!,expiresAt:r.expiry}}:{}),snapshot:s};}catch{return{allowed:false,reason:'provider_resilience_unavailable'};}}
 async observe(p:ProviderResiliencePolicy,o:ProviderResilienceOutcome,probe?:ProviderProbeLease){try{await this.ensure();const k=providerResilienceKeys(p,this.namespace),eligible=p.eligibleFailures.includes(o.classification as never),r=JSON.parse(String(await this.bounded(this.client.eval(OBSERVE,{keys:[k.state,k.probes],arguments:[o.classification,eligible?'1':'0',p.failureWindow.windowMs,p.failureThreshold,p.minimumObservations,p.openDurationMs,o.retryAfterMs??-1,p.stateTtlMs,probe?.ownerToken??'',probe?.generation??-1,probe?.expiresAt??-1].map(String)}))))as Raw;return{applied:r.applied??false,snapshot:this.snap(p,r)};}catch{return{applied:false};}}
 async releaseProbe(p:ProviderResiliencePolicy,probe:ProviderProbeLease):Promise<boolean>{try{await this.ensure();const k=providerResilienceKeys(p,this.namespace);return Number(await this.bounded(this.client.eval(RELEASE,{keys:[k.state,k.probes],arguments:[probe.ownerToken,probe.generation,probe.expiresAt,p.stateTtlMs].map(String)})))===1;}catch{return false;}}async close(){this.ready=false;if(this.client.isOpen)this.client.destroy();}}
