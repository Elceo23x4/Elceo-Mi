import type { NormalizedMarketEvidencePayload } from '@elceo/types';
import { buildMetadataJson, buildNormalizedPayloadId, buildValuesJson, clampConfidenceScore } from '../normalization-helpers';
import type { CrossMarketRateRow, EquityBreadthRow, RiskMarketFixtureRequest, RiskSentimentRow, VolatilitySurfaceRow } from './risk-market-contracts';
const f=(n:number)=>{if(!Number.isFinite(n)) throw new Error('risk_market_invalid_finite_number');};
export const buildVolatilityPayload=(r:RiskMarketFixtureRequest,row:VolatilitySurfaceRow):NormalizedMarketEvidencePayload=>{f(row.strikeDelta);f(row.impliedVolatility);const o=new Date(row.observedAt).toISOString();return {payloadId:buildNormalizedPayloadId(r.providerId,'volatility_surface',o,row.asset),evidenceTypeId:'volatility_surface',evidenceClass:'volatility_surface',providerId:r.providerId,sourceId:row.tenor,region:r.region??'global',asset:row.asset,observedAt:o,publishedAt:null,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(94),dataQuality:'high',valuesJson:buildValuesJson(row),metadataJson:buildMetadataJson({capability:r.capability})};};
export const buildRiskSentimentPayload=(r:RiskMarketFixtureRequest,row:RiskSentimentRow):NormalizedMarketEvidencePayload=>{f(row.value);const o=new Date(row.observedAt).toISOString();return {payloadId:buildNormalizedPayloadId(r.providerId,'risk_sentiment',o,null),evidenceTypeId:'risk_sentiment',evidenceClass:'risk_sentiment',providerId:r.providerId,sourceId:row.indicatorName,region:row.region,asset:null,observedAt:o,publishedAt:null,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(90),dataQuality:'high',valuesJson:buildValuesJson({region:row.region,observedAt:o,indicatorName:row.indicatorName,score:row.value,providerId:r.providerId}),metadataJson:buildMetadataJson({unit:row.unit,capability:r.capability})};};
export const buildBreadthPayload=(r:RiskMarketFixtureRequest,row:EquityBreadthRow):NormalizedMarketEvidencePayload=>{f(row.value);const o=new Date(row.observedAt).toISOString();return {payloadId:buildNormalizedPayloadId(r.providerId,'equity_index_breadth',o,row.asset),evidenceTypeId:'equity_index_breadth',evidenceClass:'equity_index_breadth',providerId:r.providerId,sourceId:row.indicatorName,region:r.region??'global',asset:row.asset,observedAt:o,publishedAt:null,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(89),dataQuality:'medium',valuesJson:buildValuesJson({region:r.region??'global',observedAt:o,indexName:row.asset,advancing:row.value*100,declining:100,breadthRatio:row.value,providerId:r.providerId}),metadataJson:buildMetadataJson({unit:row.unit,capability:r.capability})};};
export const buildCrossMarketRatePayload=(r:RiskMarketFixtureRequest,row:CrossMarketRateRow):NormalizedMarketEvidencePayload=>{f(row.value);const o=new Date(row.observedAt).toISOString();return {payloadId:buildNormalizedPayloadId(r.providerId,'cross_market_rates',o,row.asset),evidenceTypeId:'cross_market_rates',evidenceClass:'cross_market_rates',providerId:r.providerId,sourceId:row.rateName,region:r.region??'global',asset:row.asset,observedAt:o,publishedAt:null,normalizedAt:r.requestedAt,confidenceScore:clampConfidenceScore(91),dataQuality:'high',valuesJson:buildValuesJson({baseAsset:row.asset,quoteAsset:'usd',observedAt:o,value:row.value,providerId:r.providerId}),metadataJson:buildMetadataJson({unit:row.unit,capability:r.capability})};};
