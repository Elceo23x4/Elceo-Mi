import type { ScheduledIngestionRunRepository } from '../persistence/scheduled-ingestion-repository';
export class ScheduledIngestionQueryService{constructor(private readonly repo:ScheduledIngestionRunRepository){} getScheduledIngestionRunById(runId:string){return this.repo.getRunById(runId);} listScheduledIngestionRunsByJob(jobId:string,limit?:number){return this.repo.listRunsByJob(jobId,limit);} listScheduledIngestionRunsByProvider(providerId:string,capability?:string,limit?:number){return this.repo.listRunsByProvider(providerId,capability,limit);} listScheduledIngestionRunsByAsset(asset:string,limit?:number){return this.repo.listRunsByAsset(asset,limit);} listScheduledIngestionRunsByStatus(status:Parameters<ScheduledIngestionRunRepository['listRunsByStatus']>[0],limit?:number){return this.repo.listRunsByStatus(status,limit);} }
