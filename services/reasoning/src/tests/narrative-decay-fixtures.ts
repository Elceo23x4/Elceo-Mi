import type { NarrativeContinuationObservationRecord, NarrativePriceObservation } from '../narrative-decay/contracts';
import { createObservationRecord } from '../narrative-decay/identity';
import { buildCleanlinessEventFixture } from './market-cleanliness-fixtures';
export const buildNarrativeEventFixture=buildCleanlinessEventFixture;
export function buildNarrativeObservation(prices:number[],minute=30,reliability:'verified'|'fixture'|'replay'='verified'):NarrativeContinuationObservationRecord{const event=buildNarrativeEventFixture();const iso=(m:number)=>`2026-01-01T${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}:00.000Z`;const price=(asset:'xau_usd'|'dxy',values:number[]):NarrativePriceObservation=>({sourceId:`${asset}-continuation`,provider:'certified-market-data',observationVersion:`v-${minute}`,asset,reliability,candles:values.slice(1).map((close,i)=>({openedAt:iso(minute+i),closedAt:iso(minute+i+1),open:values[i]!,high:Math.max(values[i]!,close),low:Math.min(values[i]!,close),close,complete:true}))});return createObservationRecord({policyVersion:'narrative-decay-v1',expectationId:event.expectationId,eventReleaseId:event.expectation.eventReleaseId,releaseVersion:event.releaseVersion,sourceEventEvaluationId:event.eventEvaluationId,asset:event.asset,observedAt:iso(minute),availableAt:iso(minute+prices.length),evidenceCutoffAt:'2026-01-01T02:00:00.000Z',windowStartAt:iso(minute),windowEndAt:iso(minute+prices.length-1),primaryPriceObservation:price('xau_usd',prices),relatedPriceObservations:[price('dxy',prices.map((_,i)=>100+i))],cognitionSnapshotId:null,warnings:[],limitations:[]});}
